[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel.sources]
"src/" = "leaf_clustering"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[project]
name = 'leaf_clustering'
requires-python = ">= 3.10"
version = '0.0.1'

